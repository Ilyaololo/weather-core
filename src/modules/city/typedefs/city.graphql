type Query {
  city(id: String): City
  cities(q: String = ""): [City]
}

type City {
  id: String
  name: String
  region: String
  country: String
  timezone: String
  coord: Coord
}

type Coord {
  lat: Float
  lon: Float
}
