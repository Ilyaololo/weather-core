# Core

## Structure

```bash
src
├── configs         ──
├── decorators          ──
├── exceptions          ──
├── guards          ──
├── interceptors          ──
├── interfaces          ──
├── middleware          ──
├── migrations          ──
├── modules         ──
├── pipes         ──
├── providers         ──
├── templates         ──
├── typedefs          ──
├── utils         ──
└── app.ts          ──
```
